version: '3.8'

services:
  postgres:
    image: postgres:16
    container_name: hr_performance_db
    environment:
      POSTGRES_DB: hr_performance
      POSTGRES_PASSWORD: password
      POSTGRES_USER: postgres
    ports:
      - "5432:5432"